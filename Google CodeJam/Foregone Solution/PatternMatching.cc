#include <iostream>
#include <vector>

using namespace std;

int main(){
    int T;
    cin >> T;
    
    for(int i = 0; i < T; i++){
        
        int N;
        cin >> N;

        string pattern; 
        string name;

        for(int i = 0; i < N; i++){

            cin >> pattern;

        }
        cout << "Case #" << T << ": " << pattern << endl;
    }
    

    return 0;
}